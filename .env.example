# This is an example file for setting environment variables.
# Create a file named ".env" in the same directory as app.py
# and add your credentials there.

# The application will use these variables for login.
# If these are not set, it will fall back to the insecure default credentials.

ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
OPERATOR_USERNAME=operator
OPERATOR_PASSWORD=op123
